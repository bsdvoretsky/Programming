#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int S(char *c, char *T, int *j) {
	char
	for (int i = strlen(S) - 1; i >= 0; i--) {
		if ()
	}
}

void delta1(char *S, int size, int *delta) {
	int a = 0, l = strlen(S);
	while (a < size) {
		delta[a] = l;
		++a;
	}
	j = 0;
	while (j < l) {
		delta[]
	}
}


int main(int argc, char **argv)
{
	
	return 0;
}